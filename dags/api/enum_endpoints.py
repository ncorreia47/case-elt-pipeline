from enum import Enum

class Endpoints(Enum):
    ORGANIZATIONS = "organizations"
    GROUPS = "groups"
    USERS = "users"
    TICKETS = "tickets"
    TICKET_METRICS = "ticket_metrics"
    TICKET_SLA_EVENTS = "ticket_sla_events"
